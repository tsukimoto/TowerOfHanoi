package pseudocode;

public class Main {
	main(String[] args) {
		
		ハノイの塔の段数を尋ねる。
		コンソールの入力待ち。
		段数が決まる。
		
		アルゴリズムクラスの生成（描画も行う。）

		for (int i = 0; i < args.length; i++) {
			コンソールの入力待ち。
			if(エンターキーが押された)
			{
				アルゴリズムクラスで、データの移動
			}
			if(アルゴリズムクラスで終了通知)
			{
				break;
			}
		}
	}
}
